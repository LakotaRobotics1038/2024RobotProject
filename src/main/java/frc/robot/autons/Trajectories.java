package frc.robot.autons;

import com.pathplanner.lib.commands.PathPlannerAuto;
import com.pathplanner.lib.path.PathPlannerPath;

import com.pathplanner.lib.path.PathPlannerTrajectory;

import edu.wpi.first.math.geometry.Rotation2d;
import edu.wpi.first.math.kinematics.ChassisSpeeds;
import frc.robot.constants.AutoConstants;
import frc.robot.constants.DriveConstants;

public class Trajectories {
    // public static PathPlannerTrajectory NotesInAmp() {
    // return new PathPlannerTrajectory(PathPlannerAuto.getPathGroupFromAutoFile("2
    // Notes in Amp"),
    // new ChassisSpeeds(AutoConstants.kMaxSpeedMetersPerSecond / 2,
    // AutoConstants.kMaxAccelerationMetersPerSecondSquared / 2,
    // AutoConstants.kMaxAngularSpeedRadiansPerSecond / 2),
    // Rotation2d.fromDegrees(-90));
}
